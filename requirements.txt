# Pinned exactly: joblib-pickled models are not portable across sklearn
# minor versions. Training env and Streamlit Cloud must agree.
scikit-learn==1.9.0

streamlit>=1.40
pandas>=2.2
numpy>=2.0,<3
matplotlib>=3.8
seaborn>=0.13
joblib>=1.4

# Used only by model/ data-prep scripts, not by app.py at runtime.
ucimlrepo>=0.0.7
